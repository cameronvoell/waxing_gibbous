<%= form_for(@werewolf) do |f| %>
  <% if @werewolf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@werewolf.errors.count, "error") %> prohibited this werewolf from being saved:</h2>

      <ul>
      <% @werewolf.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :color %><br />
    <%= f.text_field :color %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
